{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run migration:run && npm run seed:run:relational:roles-status && npm run start:prod",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120
  }
}
